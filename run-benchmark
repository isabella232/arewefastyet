#!/bin/bash

#To be triggered within arewefastyet directory 

alias activate="benchmark/bin/activate"

#NEW_UUID=$(cat /dev/urandom | tr -dc 'a-zA-Z0-9' | fold -w 32 | head -n 1)

python3.7 run-benchmark.py $1 $2 $3
